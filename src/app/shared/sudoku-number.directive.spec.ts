import { SudokuNumberDirective } from './sudoku-number.directive';

describe('SudokuNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new SudokuNumberDirective();
    expect(directive).toBeTruthy();
  });
});
